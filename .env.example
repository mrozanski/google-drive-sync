# Google Drive Sync Configuration
# Copy this file to .env and fill in your values

# Google Drive folder ID to sync from
# You can find this in the folder URL: https://drive.google.com/drive/folders/{FOLDER_ID}
GOOGLE_FOLDER_ID=your_folder_id_here

# Path to your Google OAuth2 credentials JSON file
# Download this from Google Cloud Console
GOOGLE_CREDENTIALS_FILE=/path/to/credentials.json

# Local target directory (can use ~ for home directory)
# Example: ~/Library/Mobile Documents/com~apple~CloudDocs/Projects/MyProject
TARGET_DIRECTORY=/path/to/target/directory

# Token file location (optional, defaults to token.json in current directory)
# TOKEN_FILE=token.json
